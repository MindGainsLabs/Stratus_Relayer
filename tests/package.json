{
  "name": "stratus-relayer-websocket-tests",
  "version": "1.0.0",
  "description": "WebSocket tests for Stratus_Relayer crypto tracking API",
  "scripts": {
    "test": "mocha tests/websocket.test.js --require esm --timeout 10000",
    "test:watch": "mocha tests/websocket.test.js --require esm --timeout 10000 --watch",
    "test:verbose": "mocha tests/websocket.test.js --require esm --timeout 10000 --reporter spec"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.7",
    "socket.io-client": "^4.7.2",
    "jsonwebtoken": "^9.0.2",
    "esm": "^3.2.25"
  },
  "type": "module"
}
